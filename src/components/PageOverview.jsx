/**
 * Brief 1–3 line overview for a dashboard view. Renders muted helper text so viewers understand what the page shows.
 */
export function PageOverview({ children }) {
  return <p className="page-overview">{children}</p>;
}
